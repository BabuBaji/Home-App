{
  "name": "hh-worker-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HomeHelp Worker microservice — owns worker profiles, verification, availability, documents and the worker wallet/ledger on its own Postgres. Job-lifecycle (bookings) stays in the Booking core and calls this service for worker data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "ioredis": "^5.4.1",
    "multer": "^1.4.5-lts.1",
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/s3-request-presigner": "^3.658.0",
    "jsonwebtoken": "^9.0.2"
  }
}
